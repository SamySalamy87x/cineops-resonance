# Sites deployment metadata

The project-specific `.openai/hosting.json` file is intentionally excluded from this public repository because it identifies one deployment. Create or attach your own Sites project to generate that file locally.
